def generate_reformulations(prompt: str):
    # Aqui depois chamamos as LLMs reais
    reformulation_1 = f"Reformulação 1 baseada no prompt: {prompt}"
    reformulation_2 = f"Reformulação 2 baseada no prompt: {prompt}"
    return reformulation_1, reformulation_2
